function [pool, subsets] = tsfsb_solver_pool()
%TSFSB_SOLVER_POOL Canonical solver order and metadata for the TSFSB benchmark.
%
%   Ported from the C02 c02_solver_pool.m (commit 846d5c5f), extended to
%   the ten-solver pool. The internal labels are the machine identifiers
%   used in manifests and audit records; the display names appear on
%   OptiProfiler native figures.

pool = [ ...
    make_entry('bds', 'BDS', 'bds', ...
        'Final iterate returned by BDS'), ...
    make_entry('bds-no-acceleration', 'BDS without acceleration', ...
        'bds-without-acceleration', 'Final iterate returned by BDS'), ...
    make_entry('ds', 'DS', 'ds', ...
        'Final iterate returned by BDS with one block'), ...
    make_entry('nomad', 'NOMAD', 'nomad', ...
        'Point returned by nomadOpt'), ...
    make_entry('lam', 'LAM', 'lam', ...
        'Final paper LAM iterate returned by lam_paper_exact'), ...
    make_entry('newuoa', 'NEWUOA', 'newuoa', ...
        'Point returned by PRIMA NEWUOA'), ...
    make_entry('fd-bfgs', 'FD-BFGS', 'fd-bfgs', ...
        'Point returned by fminunc'), ...
    make_entry('pds', 'PDS', 'pds', ...
        'Final iterate returned by pds'), ...
    make_entry('bfo', 'BFO', 'bfo', ...
        'Point returned by BFO'), ...
    make_entry('nelder-mead', 'Nelder-Mead', 'nelder-mead', ...
        'Point returned by fminsearch')];

subsets = struct( ...
    'acceleration', [1, 2], ...
    'blocking', [2, 3], ...
    'direction_based', [1, 4, 5, 8, 9, 10], ...
    'model_fd', [1, 6, 7], ...
    'full_pool', 1:10);

end

function entry = make_entry(internal_label, display_name, configuration, ...
        returned_point_convention)

entry = struct( ...
    'internal_label', internal_label, ...
    'display_name', display_name, ...
    'configuration', configuration, ...
    'budget_factor', 500, ...
    'returned_point_convention', returned_point_convention);

end
